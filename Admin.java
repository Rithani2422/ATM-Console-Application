public class Admin extends Account {
    // The 'Admin' class extends the 'Account' class, meaning it inherits all properties and methods from 'Account'

    // Constructor with parameters for username, password, and balance
    public Admin(String username, String password) {
        // Call the constructor of the parent class 'Account' using the 'super' keyword
        // This initializes the 'Admin' object with the provided username, password, and balance
        super(username, password);
    }
}








