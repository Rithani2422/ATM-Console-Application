package ListOfNotes;

import Notes.Notes;

// Define a class ListOfNotes.Notes100 that extends the Notes.Notes class
public class Notes100 extends Notes {

    // Constructor for the ListOfNotes.Notes100 class that takes two parameters: Note and Count
    public Notes100(int Note, int Count) {

        // Call the superclass (Notes.Notes) constructor with the provided Note and Count values
        super(Note, Count);
    }
}
