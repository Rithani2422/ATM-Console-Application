package ListOfNotes;

import Notes.Notes;

// Define a class ListOfNotes.Notes200 that extends the Notes.Notes class
public class Notes200 extends Notes {

    // Constructor for the ListOfNotes.Notes200 class that takes two parameters: Note and Count
    public Notes200(int Note, int Count) {

        // Call the superclass (Notes.Notes) constructor with the provided Note and Count values
        super(Note, Count);
    }
}
