package ListOfNotes;

import Notes.Notes;

// Define a class ListOfNotes.Notes500 that extends the Notes.Notes class
public class Notes500 extends Notes {

    // Constructor for the ListOfNotes.Notes500 class that takes two parameters: Note and Count
    public Notes500(int Note, int Count) {

        // Call the superclass (Notes.Notes) constructor with the provided Note and Count values
        super(Note, Count);
    }
}
